<div class="col-md mt-3">
  <ul class="list-group">
    <h4><%= title %>:</h4>
    <% list.each do |item| %>
      <li class="list-group-item d-flex justify-content-between align-items-center">
        <%= link_to item.send(attribute), item %>
        <span style="font-size: medium;" class="badge badge-primary badge-pill">😎</span>
      </li>
    <%end%>
  </ul>
</div>
